import React from "react";

import Layout from "$components/Layout";

const OrgCatalog = () => {
  return (
    <Layout>
      <div>org catalog</div>
    </Layout>
  );
};

export default OrgCatalog;
